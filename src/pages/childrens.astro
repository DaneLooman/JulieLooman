---
import Layout from '../layouts/Layout.astro';
import top from '../assets/WVUM-Hero.webp';
import middle from '../assets/WVUM-Pages.webp';
import bottom from '../assets/WVUM-Color.webp';
import alt from '../assets/Index-WVUM.webp';
---

<Layout>
	<main>
		<div id='hero'>
			<h1>
				Illustration,<br />
				Print Design
			</h1>
		</div>
		<div id='text'>
			<h2>Pediatric surgery can be scary, let's make learning about it fun.</h2>
			<p>
				Illustrated and hand crafted, this coloring book takes the patient and
				their parent through the process of surgery. It explains the different
				steps that will happen, while providing activities to think on the
				process. <br /><br />
				<span class='thin'>WVU Medicine Children's</span>
			</p>
		</div>
		<div id='middle'></div>
		<div id='bottom'></div>
	</main>
</Layout>

<style
	define:vars={{
		top: `url(${top.src})`,
		middle: `url(${middle.src})`,
		bottom: `url(${bottom.src})`,
		alt: `url(${alt.src})`,
	}}
>
	main {
		box-sizing: border-box;
		display: grid;
		grid-template-columns: 1fr 1fr;
		width: 100%;
	}
	#hero {
		box-sizing: border-box;
		grid-column: 1 / span 2;
		background-image: var(--top);
		height: 90vh;
		display: grid;
		align-items: end;
		color: #fff;
		padding: 60px;
		font-size: 60px;
		background-size: cover; /* Resize the background image to cover the entire container */
	}
	#hero > h1 {
		font-size: 60px;
		stroke: rgba(48, 79, 116, 1);
	}
	#text {
		font-family: 'Montserrat';
		color: rgba(48, 79, 116, 1);
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
	}
	#text > * {
		max-width: 400px;
	}
	#text > h2 {
		font-weight: 700;
	}
	#text > p {
		font-weight: 400;
		font-size: 18px;
		line-height: 30px;
		letter-spacing: -1.5%;
	}
	#text > p > .thin {
		padding-top: 16px;
		font-weight: 100;
		color: rgba(48, 79, 116, 0.8);
	}
	#middle {
		width: 100%;
		height: 90vh;
		background-image: var(--middle);
		height: 90vh;
		background-size: cover; /* Resize the background image to cover the entire container */
	}
	#bottom {
		grid-column: 1 / span 2;
		width: 100%;
		height: 90vh;
		background-image: var(--bottom);
		height: 90vh;
		background-size: cover; /* Resize the background image to cover the entire container */
	}
	@media only screen and (max-width: 843px) {
		main {
			grid-template-columns: 1fr;
		}
		#hero,
		#bottom {
			grid-column: 1 / 2;
			background-position: center; /* Center the image */
		}
		#hero {
			padding: 8px;
			background-image: var(--alt);
		}
		#hero > h1 {
			font-size: 40px;
			stroke-width: 2px;
			-webkit-text-stroke-width: 2px;
			-webkit-text-stroke-color: rgba(48, 79, 116, 1);
		}
		#text {
			padding: 32px 48px;
		}
	}
</style>
